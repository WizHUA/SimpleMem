# 交付验证记录

2026-09-19 追加条件检索验证：后端全套 **216 passed**；三路实际并发、单字段跳过向量、字段失配回退、确定性合并、超时与故障取消均通过。Ruff 检查与格式、TypeScript/Vite 构建通过，Playwright 检索相关测试 **6 passed**。500 条合成记忆、7 次暖查询、40 ms 模拟向量延迟下，精确查值中位耗时为 12.914 ms，串行语义对照为 76.584 ms；开放问题约 62 ms，没有测出稳定提速。方法、限制和复现命令见 [三路条件检索](CONDITIONAL_RETRIEVAL.md)。本次未调用真实向量服务，不替代下述历史真实模型验收。

2026-09-17，`full-forward`，后端版本 `0.3.3`。测试使用仓库指定 `simplemem-agentmemory` 环境；运行库版本见 [requirements.lock](../requirements.lock)。本轮范围不含 Engine 集成。本记录说明已执行范围，不替代目标环境验收。

| 检查 | 实际结果 | 范围 |
|---|---|---|
| Python 测试 | 199 passed | 存储、自动提取/演化、FTS/向量投影、别名链、维护、恢复、取消、隔离、HTTP及原SDK契约 |
| Ruff 检查与格式 | 通过 | `src tests scripts` |
| 前端 TypeScript 与 Vite 构建 | 通过 | 生成可部署静态产物 |
| Playwright 交互 | 20 passed | 测试夹具，含引用弹窗与键盘焦点、Markdown代码/链接隔离、历史来源稳定、NDJSON中文分块、真实阶段/回放区分、归档恢复、错误恢复及窄屏 |
| Playwright真实后端集成 | 1 passed | 真实FastAPI/临时SQLite，Bearer、版本替代、审计、撤回、幂等 |
| 本轮动态检索/真实加速观测 | 通过 | 省略top_k自适应，通道状态真实；固定上下文disabled→miss→hit，详见 [观测报告](reports/ADAPTIVE_OBSERVATION.md) |
| 本轮回答与来源体验 | 通用提示词5/5首次通过 | 真实模型空白zfc、未知昵称、zfc直接回答、当前值更正、主动询问来源；[原始答复](reports/ANSWER_UX_QUALITY.md) |
| 真实模型冒烟 | 通过 | 抽取两条长期记忆、跨会话召回、生成缓存命中，见 [LIVE_SMOKE](LIVE_SMOKE.md) |
| 真实模型实用会话 | 严格规则13/14，0请求错误 | 初轮12/14归档；zfc回忆/纠错、跨会话、别名、临时覆盖及超窗口长会话，见 [PRACTICAL_QUALITY](PRACTICAL_QUALITY.md) |
| 旧版十题小集 | 历史记录9/10 | 0.2版本结果，未冒充本轮重跑；见 [LIVE_QUALITY](LIVE_QUALITY.md) |
| 1万条索引对照 | 暖投影中位177.07 ms，对照1152.44 ms | 5次合成测试、相同命中目标；含首次建索引边界见 [INDEX_BENCHMARK](INDEX_BENCHMARK.md) |
| 真实前端与真实模型 | 通用提示词下通过zfc告知→再询问 | 实际回答与引用弹窗/过程截图见 [视觉验收](../../memory-ui/docs/screenshots/README.md)；未使用浏览器回答夹具 |
| 合成检索与加速 | 已执行并保留不足 | 词法改写挑战不通过；生成调用合并有效，见 [EVALUATION](EVALUATION.md) |
| ASGI 30/100并发 | 失败率0%，重启记录390/390 | 进程内负载，不是网络SLA，见 [HTTP_LOAD](HTTP_LOAD.md) |
| 前端依赖审计 | 未发现已知漏洞 | 实际执行 `pnpm audit --audit-level high` |
| 后端依赖审计 | 未发现已知漏洞 | pip-audit 2.10.1 扫描锁定的17个运行时包；不代表无未知漏洞 |
| 后端wheel构建/独立导入 | 通过 | 可安装分发包；核心无课程宿主依赖 |
| 真实课程宿主 | 未执行 | 现有工作区和参考包缺完整宿主；已有 [真实验证脚本](../scripts/validate_host_sdk.py) |

Python测试有一条来自 Starlette 测试客户端的 AnyIO 弃用提示；前端终端有颜色环境变量提示，均未影响通过结果。

真实模型结果来自真实调用与合成测试数据，不是公开基准或用户长期对话数据集。新14题的剩余失败中，检索来源为当前active v2“梧桐会议室”，回答也给出该值，但附带旧地点，违反预设禁用旧词规则；仍保留13/14，不将复核改写成满分。旧十题第9题的日期问题属于另一轮历史实验，不合并统计。

本轮追加来源快照测试：同主体同会话正文绑定、禁止伪造来源、逐消息持久化、幂等重试、200条待绑定记录回收、删除/更正/有效期边界阻止旧结果写入；流式查询传递真实计划、逐层召回和最终证据，完成事件在可信快照保存成功后才发送。模型提示词与抽取预算均排除引用快照。

前轮新增故障路径验证：抽取成功后演化失败通过持久outbox恢复，跨重启不重复抽取；问句截断引文及助手回复不能洗成事实；归档/恢复往返后旧revision被拒绝；摘要自然过期不再展示；删库与取消并发时旧快照不能回填投影；实际ASGI断连取消模型且不发送伪result；删除请求重复取消仍等候权威库和派生数据清除。周期维护使用真实过期事件验证执行与关闭。

加速消融合成测试为10次重复仅1次生成、8并发仅1次生成。真实模型冒烟热命中避免1次最终生成，规划仍每次执行。真实向量服务未配置，向量投影由可控协议/并发测试验证，不能将词法改写挑战写成语义检索已验收。

## 交付包

`dist/full-forward-delivery.zip` 包含白名单源码、测试、文档、前端构建、当前后端wheel及 `MANIFEST.sha256.json`。不包含私有 `.env`、运行数据库、模型密钥或依赖目录。通过 `scripts/package_delivery.py` 重建。

部署前仍需课程宿主、实际身份系统、HTTPS、目标容量、真实 embedding 服务、数据保留策略和恢复演练验收。当前实现是受控单节点交付版本，未声称已验证多副本水平扩展。

本轮新增：动态K不受前端固定5条或API默认10条限制，实际三路分库统计，会话列表认证隔离及服务端更新时间排序。浏览器新增拖拽/键盘侧栏宽度、收起状态恢复、会话切换与草稿恢复、三路禁用/跳过状态及动态预算展示。

0.3.3追加：同主体同作用范围的对象整合视图、冲突属性隔离、逐通道真实候选快照、权限和有效期筛选、候选截断与最终ID/version/revision定位。浏览器验证同对象两属性合成单卡且原文可分别展开，实际检索条件/未命中内容/保存上限明确。

0.3.3追加方案生成链路：规则 planner 可识别通用实施计划、应急行动计划、方案与任务指令卡三类请求，并固化问题拆分、所需信息、信息梳理、整合步骤和行动要素模板；回答生成 prompt 已验证包含“需求拆分 -> 所需信息梳理 -> 信息整合 -> 完整方案”的输出契约。方案与任务指令卡的 HTTP 端到端离线用例验证了 `# 方案名称`、`## 作战概述`、`## 兵力部署`、兵力编成表格、`## 情景假设与分案`、`## 任务指令卡` 和交战规则/约束字段；同时验证了不再把整份方案大量写成“待补充”，而是按检索事实、情景假设、多分案和“需确认”问题组织输出。
